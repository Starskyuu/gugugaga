# 城市方舟项目目录说明

本目录按照“资料、研发运行、数据与模型、交付物、过程产物”分类。为了避免破坏已有引用、Git记录和正在运行的服务，原有编号目录以及`frontend`、`run`等核心目录保持原路径。

## 课程和研究资料

- `01 papers`：论文和研究资料。
- `02 reading notes`：阅读笔记。
- `03 课程要求`：课程要求。
- `04 目前进展`：阶段进展材料。
- `05 开题报告`：开题报告材料。
- `06 每日记录`：过程记录。
- `10 现实难题`：现实问题整理。
- `作业`：课程作业。

## 仿真和计算

- `07cn仿真`：中文仿真项目。
- `07enSimulation`：英文仿真项目。
- `07enSimulation_v2`：英文仿真第二版。
- `08 搜救计算`：搜救计算内容。
- `blender`：Blender相关内容。

## 软件运行

- `09 software`：已有软件实现。
- `frontend`：城市方舟实时控制前端。
- `run`：ESP32控制程序、电脑或树莓派视觉程序、模型和摄像头标定。

## 数据和训练

- `labels`：标注数据。
- `train-5`：模型训练结果或训练过程文件。
- `yolo_photos`：YOLO图像数据。
- `zdx`：相关数据或标注内容。

## 文档

- `docs/城市方舟实时搜救系统使用手册.md`：系统安装、运行、联调和排障手册。

## 部署

- `deploy/raspberry_pi_package_20260908`：树莓派部署副本目录。
- `deploy/raspberry_pi_package_20260908.zip`：可直接传输的树莓派部署压缩包。

## 日志

- `logs/collision`：船队和单船碰撞测试、回归测试日志。

## 工具

- `tools/daily_record`：每日记录生成、修订和文档检查脚本。

## 归档

- `archives/datasets/labels-zdx.zip`：标签数据压缩归档。

## 过程产物

- `ppt_build`：演示文稿构建、检查和渲染产物，包含GPS YOLO定位方案检查结果。
- `tmp`：既有临时渲染和验证内容，因仍包含文件而保留。
- `.workbuddy`：既有工具工作目录。

## 保持在根目录的文件

- `.gitignore`：Git忽略规则，必须保留在仓库根目录。
- `09 software.zip`：Git已跟踪文件，为避免改变仓库历史路径而保留。
- `项目目录说明.md`：本目录索引。

## 核心启动路径

视觉服务：

```powershell
cd F:\gugugaga\run
python 6.py --no-display --host 127.0.0.1 --port 8080
```

前端：

```powershell
cd F:\gugugaga\frontend
npm run dev
```

本次整理没有移动`frontend`和`run`，因此现有启动命令和手册中的核心路径仍然有效。
